class BrainLocalVoiceDetection {
    constructor(options) {
        console.log("Creating local voice detection with options", options)
    }

    start = () => {
        console.log("Starting local voice detection");

    }
}